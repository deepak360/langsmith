Concept in Langsmith:
1. Project
2. Trace
3. Run

user -> prompt -> LLM -> Parser  = This whole is called project
execution in every step (prompt/LLM/Parser) = This is called Trace
every component (prompt/LLM/Parser) = This is called RUN(during every trace every component RUN thet is called RUN)
